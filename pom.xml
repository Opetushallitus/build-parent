<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- default build configuration for all projects and modules, can be overridden -->

    <groupId>fi.vm.sade.generic</groupId>
    <artifactId>build-parent</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jetty.module>${artifactId}</jetty.module>
        <jetty.webApp>../${jetty.module}/target/${jetty.module}.war</jetty.webApp>
        <generic.version>1.0-SNAPSHOT</generic.version>
        <cxf.version>2.4.6</cxf.version>
        <camel.version>2.8.3</camel.version>
        <vaadin.version>6.7.5</vaadin.version>
        <gwt.version>2.3.0</gwt.version>
        <gwt.plugin.version>2.2.0</gwt.plugin.version>
        <spring.version>3.0.6.RELEASE</spring.version>
        <sade-generic.version>1.0-SNAPSHOT</sade-generic.version>
        <jodatime.version>1.6.2</jodatime.version>
        <dbunit.version>2.4.8</dbunit.version>
        <slf4j.version>1.6.1</slf4j.version>
        <jpa-api.version>1.99.0</jpa-api.version>
        <atomikos.version>3.6.6</atomikos.version>

        <project.svn.base>https://penaali.hard.ware.fi/svn/generic/${artifactId}</project.svn.base>
    </properties>

     <scm>
        <connection>scm:svn:${project.svn.base}/trunk</connection>
        <developerConnection>scm:svn:${project.svn.base}/trunk</developerConnection>
        <url>${project.svn.base}</url>
    </scm>

    <distributionManagement>
        <repository>
            <id>internal-nexus</id>
            <url>http://penaali.hard.ware.fi/nexus/content/repositories/releases/</url>
        </repository>
        <snapshotRepository>
            <id>internal-nexus-snapshots</id>
            <url>http://penaali.hard.ware.fi/nexus/content/repositories/snapshots/</url>
        </snapshotRepository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>com.springsource.repository.bundles.release</id>
            <name>SpringSource Enterprise Bundle Repository - SpringSource Bundle Releases</name>
            <url>http://penaali.hard.ware.fi/nexus/content/repositories/springsource-ebr/</url>
        </repository>
        <repository>
            <id>com.springsource.repository.bundles.external</id>
            <name>SpringSource Enterprise Bundle Repository - External Bundle Releases</name>
            <url>http://penaali.hard.ware.fi/nexus/content/repositories/spring-ebr/</url>
        </repository>
        <repository>
            <id>atomikos</id>
            <name>Atomikos repository</name>
            <url>http://penaali.hard.ware.fi/nexus/content/repositories/atomikos/</url>
        </repository>
        <repository>
            <id>central</id>
            <name>Central Maven Repository</name>
            <url>http://penaali.hard.ware.fi/nexus/content/repositories/central/</url>
        </repository>
    </repositories>

    <build>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>

        <!--<extensions>-->
            <!--<extension>-->
                <!--<groupId>fi.vm.sade.generic</groupId>-->
                <!--<artifactId>generic-maven-extension</artifactId>-->
                <!--<version>1.0-SNAPSHOT</version>-->
            <!--</extension>-->
        <!--</extensions>-->

        <pluginManagement>
            <plugins>
<!--
                <plugin>
                    <groupId>org.mortbay.jetty</groupId>
                    <artifactId>maven-jetty-plugin</artifactId>
                    <version>6.1.10</version>
                    <configuration>
                        <scanIntervalSeconds>1</scanIntervalSeconds>
                        <stopKey>foo</stopKey>
                        <stopPort>9999</stopPort>
-->
                        <!-- false if we want to keep jetty running -->
<!--
                        <daemon>false</daemon>
                    </configuration>
                </plugin>
-->
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.3.2</version>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>1.0.1</version>
                <executions>
                    <execution>
                        <id>enforce-java</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[1.6,1.6.1]</version>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Attach sources with distribute package -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>2.1.2</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- separate unit tests and integration/system tests -->
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/selenium/**</exclude>
                        <exclude>**/it/**</exclude>
                        <exclude>**/itest/**</exclude>
                        <exclude>**/stest/**</exclude>
                        <exclude>**/systemtest/**</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>2.6</version>
                <configuration>
                    <includes>
                        <include>**/selenium/**</include>
                        <include>**/it/**</include>
                        <include>**/itest/**</include>
                        <include>**/stest/**</include>
                        <include>**/systemtest/**</include>
                    </includes>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- print active profiles -->
            <!-- TODO: näyttää profiilit vain current pomista, ei parenttien
            <plugin>
                <artifactId>maven-help-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>active-profiles</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            -->

            <plugin>
                <!-- explicitly define maven-deploy-plugin after other to force exec order -->
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.7</version>
                <executions>
                    <execution>
                        <id>deploy</id>
                        <phase>deploy</phase>
                        <goals>
                            <goal>deploy</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>2.2.1</version>
                <configuration>
                    <tagBase>${project.svn.base}/releases</tagBase>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>2.9.1</version>
                <executions>
                    <execution>
                        <id>check</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
                <!--
                <configuration>
                    <configLocation>checkstyle-oph.xml</configLocation>
                </configuration>
                -->
                <dependencies>
                    <dependency>
                        <groupId>fi.vm.sade.generic</groupId>
                        <artifactId>build-utilities</artifactId>
                        <version>1.0-SNAPSHOT</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!--<plugin>-->
                <!--<groupId>com.googlecode.maven-java-formatter-plugin</groupId>-->
                <!--<artifactId>maven-java-formatter-plugin</artifactId>-->
            <!--</plugin>-->
            <!--<version>0.3.1</version>-->

        </plugins>
    </build>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>2.9.1</version>
                <configuration>
                    <configLocation>checkstyle-oph.xml</configLocation>
                </configuration>
            </plugin>
        </plugins>
    </reporting>

    <profiles>

        <profile>
            <id>jetty</id>
            <activation>
                <file>
                    <exists>profile.jetty</exists>
                </file>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.mortbay.jetty</groupId>
                        <artifactId>maven-jetty-plugin</artifactId>
                        <configuration>
                            <webApp>${jetty.webApp}</webApp>
                            <contextPath>/${jetty.module}</contextPath>
                            <!-- false if we want to keep jetty running -->
                            <daemon>true</daemon>
                            <!--<daemon>false</daemon>-->
                            <stopKey>stop</stopKey>
                            <stopPort>8888</stopPort>
                        </configuration>
                        <executions>
                            <execution>
                                <id>start-jetty</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>run-war</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>stop-jetty</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>stop</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <!--
                    <plugin>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <executions>
                            <execution>
                                <phase>compile</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <tasks>
                                        <echo message=" ***** JETTY PROFILE ACTIVE *****"/>
                                    </tasks>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    -->
                </plugins>
            </build>

        </profile>

        <!-- servicemix installation profile - TODO: replace with more elegant solution when available -->
        <profile>
            <id>servicemix-install</id>
            <activation>
                <file>
                    <exists>profile.servicemix</exists>
                </file>
            </activation>
            <build>
                <plugins>

                    <!-- run servicemix -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>1.2.1</version>
                        <executions>
                            <execution>
                                <id>exec-servicemix-start</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>java</goal>
                                </goals>
                                <configuration>
                                    <mainClass>fi.vm.sade.servicemix.ServicemixStart</mainClass>
                                    <arguments>
                                        <argument>${project.basedir}/target/servers/apache-servicemix-4.4.1/bin</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>exec-servicemix-stop</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>java</goal>
                                </goals>
                                <configuration>
                                    <mainClass>fi.vm.sade.servicemix.ServicemixStop</mainClass>
                                    <arguments>
                                        <argument>${project.basedir}/target/servers/apache-servicemix-4.4.1/bin</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- TODO: antrun not so elegant but works for now, replace with some dependency system -->
                    <plugin>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>servicemix-install-server</id>
                                <!-- package phase so this runs before smx start, which is at pre-integration-test -->
                                <phase>package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <tasks>
                                        <taskdef resource="net/sf/antcontrib/antcontrib.properties" classpathref="maven.plugin.classpath"/>
                                        <if>
                                            <os family="windows"/>
                                            <then>
                                                <get src="http://www.nic.funet.fi/pub/mirrors/apache.org/servicemix/servicemix-4/4.4.1/apache-servicemix-4.4.1.zip" dest="target/apache-servicemix-4.4.1.zip"/>
                                                <unzip src="target/apache-servicemix-4.4.1.zip" dest="target/servers"/>
                                                <copy file="karaf.bat" todir="target/servers/apache-servicemix-4.4.1/bin" failonerror="false"/>
                                                <copy file="servicemix.bat" todir="target/servers/apache-servicemix-4.4.1/bin" failonerror="false"/>
                                            </then>
                                            <else>
                                                <get src="http://www.nic.funet.fi/pub/mirrors/apache.org/servicemix/servicemix-4/4.4.1/apache-servicemix-4.4.1.tar.gz" dest="target/apache-servicemix-4.4.1.tar.gz"/>
                                                <gunzip src="target/apache-servicemix-4.4.1.tar.gz" dest="target/apache-servicemix-4.4.1.tar"/>
                                                <untar src="target/apache-servicemix-4.4.1.tar" dest="target/servers"/>
                                                <copy file="karaf" todir="target/servers/apache-servicemix-4.4.1/bin" failonerror="false"/>
                                                <copy file="servicemix" todir="target/servers/apache-servicemix-4.4.1/bin" failonerror="false"/>
                                                <chmod file="target/servers/apache-servicemix-4.4.1/bin/servicemix" perm="755" verbose="true"/>
                                                <chmod file="target/servers/apache-servicemix-4.4.1/bin/karaf" perm="755" verbose="true"/>
                                                <chmod file="target/servers/apache-servicemix-4.4.1/bin/start" perm="755" verbose="true"/>
                                                <chmod file="target/servers/apache-servicemix-4.4.1/bin/stop" perm="755" verbose="true"/>
                                            </else>
                                        </if>

                                        <!-- copy bundles/features - TODO: hack, depsuina! -->
                                        <copy todir="target/servers/apache-servicemix-4.4.1/deploy" verbose="true">
                                            <fileset dir="${servicemix.features.xml.path}" includes="*.xml"/>
                                        </copy>

                                    </tasks>
                                </configuration>
                            </execution>
                        </executions>
                        <dependencies>
                            <dependency>
                                <groupId>ant-contrib</groupId>
                                <artifactId>ant-contrib</artifactId>
                                <version>1.0b3</version>
                                <exclusions>
                                    <exclusion>
                                        <groupId>ant</groupId>
                                        <artifactId>ant</artifactId>
                                    </exclusion>
                                </exclusions>
                            </dependency>
                        </dependencies>
                    </plugin>

                </plugins>
            </build>
        </profile>

    </profiles>

  <modules>
    <module>build-utilities</module>
  </modules>
</project>